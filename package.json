{
  "name": "lunchhunch",
  "version": "1.0.0",
  "description": "Find out what's for lunch!",
  "main": "server.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm install --only=dev && npm install && npm run client\"",
    "server": "node server.js",
    "client": "node start-client.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mike Radzwilla",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "yelp-fusion": "^2.0.3"
  },
  "devDependencies": {
    "concurrently": "^3.5.1"
  }
}
